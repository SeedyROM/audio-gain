{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/bin/audio-gain",
      "preLaunchTask": "Build",
      "args": [
        "-i", 
        "../wav-files/tengo.wav",
        "-o", 
        "./test.wav",
        "-g",
        "0.88"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}